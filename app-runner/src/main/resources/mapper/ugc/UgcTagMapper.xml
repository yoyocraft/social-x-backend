<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youyi.domain.ugc.repository.mapper.UgcTagMapper">

    <resultMap id="ConfigResultMap" type="com.youyi.domain.ugc.repository.po.UgcTagPO">
        <id column="id" property="id"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
        <result property="tagId" column="tag_id"/>
        <result property="tagName" column="tag_name"/>
        <result property="priority" column="priority"/>
        <result property="type" column="type"/>
        <result property="creatorId" column="creator_id"/>
    </resultMap>

    <sql id="result_cols">
        id, gmt_create, gmt_modified, tag_id, tag_name, creator_id, priority, type
    </sql>
    <insert id="insert">
        INSERT INTO `ugc_tag` (tag_id, tag_name, creator_id, priority, type)
        VALUES (#{po.tagId}, #{po.tagName}, #{po.creatorId}, #{po.priority}, #{po.type})
    </insert>
    <insert id="insertBatch">
        INSERT INTO `ugc_tag` (tag_id, tag_name, creator_id, priority, type)
        VALUES
        <foreach collection="tags" item="item" index="index" separator=",">
            (#{item.tagId}, #{item.tagName}, #{item.creatorId}, #{item.priority}, #{item.type})
        </foreach>
    </insert>
    <select id="queryByType" resultType="com.youyi.domain.ugc.repository.po.UgcTagPO">
        SELECT
        <include refid="result_cols"/>
        FROM ugc_tag
        WHERE type = #{type}
        ORDER BY priority DESC, id DESC
    </select>
    <select id="queryAll" resultType="com.youyi.domain.ugc.repository.po.UgcTagPO">
        SELECT
        <include refid="result_cols"/>
        FROM ugc_tag
        ORDER BY priority DESC, id DESC
    </select>
    <select id="queryByCursor" resultType="com.youyi.domain.ugc.repository.po.UgcTagPO">
        SELECT
        <include refid="result_cols"/>
        FROM ugc_tag
        WHERE id <![CDATA[ >= ]]> #{cursor}
        ORDER BY id ASC, priority DESC
        LIMIT #{size}
    </select>


</mapper>
